## Summary

<!-- What changed and why (1–3 bullets). -->

## Test plan

- [ ] `go test ./...` and `go vet ./...`
- [ ] Restart `./wabot` locally; smoke new HTTP routes if added

## wabot-agent

<!-- Link the matching wabot-agent PR if agent tools depend on new routes. -->
